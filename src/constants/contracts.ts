import dotenv from 'dotenv';

dotenv.config();

// export const OracleContract = process.env.ORACLE_CONTRACT;

export const RouletteContract = process.env.ROULETTE_CONTRACT;

export const NFTContract = process.env.NFT_CONTRACT;

export const FuzioOptionContract = process.env.FUZIO_OPTION_CONTRACT;

export const AirdropContract = process.env.AIRDROP_CONTRACT;
